apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: liatrio-app-ingress
  namespace: default
spec:
  defaultBackend:
    service:
      name: liatrio-app-deployment-web-service
      port:
        number: 8080